<script>
console.log("yo")
$.notify({
	// options
	message: '<%= Group.find(groupid).title %>:<br /><%= User.find(userid).user_name %> says <%= message.body %>',
  url: '<%= group_path(Group.find(groupid)) %>'
},{
	// settings
	type: 'danger'
});
</script>
