<script>
console.log("yo")
$.notify({
	// options
	message: '<%= Group.find(groupid).title %>: <%= message.body %>',
  url: '<%= group_path(Group.find(groupid)) %>'
},{
	// settings
	type: 'danger'
});
</script>
